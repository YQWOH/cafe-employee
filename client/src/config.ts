const API_BASE_URL = 'http://localhost:8800/api';

export default {
    CAFES: `${API_BASE_URL}/cafes`,
    CAFE: `${API_BASE_URL}/cafe`,
    EMPLOYEES: `${API_BASE_URL}/employees`,
    EMPLOYEE: `${API_BASE_URL}/employee`
};
